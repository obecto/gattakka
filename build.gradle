plugins {
    id 'scala'
}

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'org.scala-lang', name: 'scala-library', version: scala_version
    compile group: 'com.typesafe.akka', name: 'akka-actor_2.12', version: akka_version
}